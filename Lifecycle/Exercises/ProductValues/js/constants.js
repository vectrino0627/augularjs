(function(angular) {
  var mod = angular.module( 'productConstants', [] );

  // Create a constant 'urlBase' which returns the base URL for the application
  // (Probably '/products')

})(angular);