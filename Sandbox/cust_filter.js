/**
 * Created by nuo134 on 12/16/2015.
 */

/*
function foo(inputArray, fieldName, searchString)
{
    var outputArray =[];
    inputArray.forEach(function(record){
        if (record[fieldName] && )
    })


}


        mod.filter('startWith', function(){

           return function(inputArray, fieldName, searchString)
           {
             var oarray  = [];
               inputArray.forEach( function (record){
                   if (record[fieldName] && record[fieldName].startsWith(searchString)){
                       oarray.push(record);
                   }
               });
           };

        });*/

    //<li ng-repeat = "   | startswith:'name':stateName" ></li>
